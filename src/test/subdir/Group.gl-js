function main() {
    Group.run({
        scenario: test,
        profile: [
            {target: 100, duration: '5s', shape: Shape.smooth},
            {target: 0,   duration: '5s', shape: Shape.smooth}
        ]
    });
}

function test() {
    http(demoServer)
        .get('')
        .get('speed/slow')
        .sync();
}